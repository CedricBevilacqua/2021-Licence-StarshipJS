export default class MoveState {
    static HAUT(){ return HAUT; }
    static BAS(){ return BAS; }
    static DROITE(){ return DROITE; }
    static GAUCHE(){ return GAUCHE; }
    static IMMOBILE(){ return IMMOBILE; }
}